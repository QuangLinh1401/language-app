{
  "name": "language-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "English learning app — React frontend + Express API (serverless on Vercel) + Neon Postgres.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
